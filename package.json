{
  "name": "kiss-google-cloud-client",
  "version": "0.9.0",
  "description": "Simple HTTP client for google cloud service integration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peracto <garyranson@hotmail.com>",
  "license": "ISC",
  "dependencies": {
    "kiss-auth-fetch": "^0.9.0",
    "kiss-jwt": "^0.9.0",
    "kiss-retry-fetch": "^0.9.0"
  }
}
